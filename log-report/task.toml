artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log and generate a JSON summary report."

[verifier]
timeout_sec = 120

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = ["transform", "generate"]
artifact_type = "generated_output_artifact"
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "The task requires parsing an Apache access log, counting requests, determining unique client IPs, and identifying the most requested path."
solution_explanation = "Parse each log entry, compute total requests, unique IP count, and the most frequently requested path, then write the required JSON report."
verification_explanation = "The verifier checks that report.json exists and that each required JSON field has the correct value."

[environment]
allow_internet = false
mcp_servers = []

[agent]
timeout_sec = 120